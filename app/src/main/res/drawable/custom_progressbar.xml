<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:angle="270"
                android:centerColor="@color/colorProgressBarback"
                android:centerY="0.5"
                android:endColor="@color/colorProgressBarback"
                android:startColor="@color/colorProgressBarback" />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:angle="270"
                    android:centerColor="@color/colorProgressProgress"
                    android:centerY="0.5"
                    android:endColor="@color/colorProgressProgress"
                    android:startColor="@color/colorProgressProgress" />
            </shape>
        </clip>
    </item>
</layer-list>